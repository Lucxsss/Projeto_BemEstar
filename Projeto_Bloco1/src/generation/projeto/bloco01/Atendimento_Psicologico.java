package generation.projeto.bloco01;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

public class Atendimento_Psicologico {
	Presencial pre = new Presencial();

	public Presencial getPre() {
		return pre;
	}

	public void setPre(Presencial pre) {
		this.pre = pre;
	}
	
		/*public String sorteia(){

			List<String> lista = new ArrayList<String>() ;
			lista.add (pre.getClinon01());
			lista.add (pre.getClinon2());
			lista.add (pre.getClinon3());
			lista.add (pre.getClinon4());
			lista.add (pre.getClinon5());
			lista.add (pre.getClinon6());
			lista.add (pre.getClinon7());
			lista.add (pre.getClinon8());
			lista.add (pre.getClinon9());
			lista.add (pre.getClinon10());

			Collections.shuffle(lista) ;
			return lista.get((int)Math.random() * lista.size());*/
		}
	
